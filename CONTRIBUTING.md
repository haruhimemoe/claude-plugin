# Contributing

Read [AGENTS.md](AGENTS.md) first: it has the rules for writing and testing a skill. Open an issue before a big change. Every pull request needs `node scripts/validate.mjs` and both `claude plugin validate` checks to pass.
