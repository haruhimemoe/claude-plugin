---
max_turns: 12
allowed_tools: [Skill]
tags: [pool]
---

In my React app I call `setPool(addSlot(pool, "NM", id))` from @haruhimemoe/pool. Sometimes nothing changes on screen and nothing throws. What's going on and how should the UI handle it?
